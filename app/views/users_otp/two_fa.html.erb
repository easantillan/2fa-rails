<h1>Bienvenido</h1>
<p>Por favor confirma tu sesión con el código que hemos enviado por correo electrónico</p>
<%= form_for(resource, as: resource_name, url: session_path(resource_name), method: :post) do |f| %>
  <div><%= f.label :otp_attempt, "Código de seguridad:" %><br />
    <%= f.text_field :otp_attempt %> </div>

  <%= f.submit "Enviar" %>
<%end %>